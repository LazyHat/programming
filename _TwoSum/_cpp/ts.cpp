#include <vector>

using namespace std;

vector<int> twoSum(vector<int> &nums, int target)
{

    for (int i = 0; i < nums.size(); i++)
    {
        if (nums.)
    }
    return {-1, -1};
}

int main()
{
    vector<int> a;
    for (int i = 1; i < 10; i++)
        a.push_back(i);

    auto target = 4;

    auto result = twoSum(a, target);
    return 0;
}